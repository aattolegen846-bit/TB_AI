asynctest
codecov
flake8
interrogate
isort==4.3.21
# Note: used for kwarray.group_items, this may be ported to mmcv in the future.
kwarray
mmtrack
# Relax the pinned onnx to allow modern prebuilt wheels on macOS/Linux
onnx>=1.12.0
onnxruntime>=1.8.0
# Pin a wheelable pycocotools to avoid source build failures
pycocotools==2.0.11
pytest
ubelt
xdoctest>=0.10.0
yapf
